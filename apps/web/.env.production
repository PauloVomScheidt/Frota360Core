# Base da API em produção. Precisa terminar em /api/v1: os módulos de src/api chamam caminhos
# relativos ("/veiculo", "/auth/login") sobre este baseURL.
#
# O Vite embute este valor no bundle em tempo de build — alterar depois exige rebuild e novo
# deploy do front.
VITE_API_URL=https://api.frota360app.com.br/api/v1
